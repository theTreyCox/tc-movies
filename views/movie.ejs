<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="local-item-details">
        <h1><%= movie.title %> (<%= movie.release_year %>)</h1>
        <hr>
        <h2>Details for Collection Item</h2>
        <p><strong>Format:</strong> <%= movie.format %></p>
        <p><strong>Label:</strong> <%= movie.label %></p>
        <p><strong>Packaging:</strong> <%= movie.packaging %></p>
        <p><strong>Slipcase:</strong> <%= movie.slipcover ? 'Yes' : 'No' %></p>
    </div>

    <% if (details) { %>
        <hr>
        <div class="omdb-details">
            <h2>Details from OMDb</h2>
            <p><strong>Plot:</strong> <%= details.Plot %></p>
            <p><strong>Genre:</strong> <%= details.Genre %></p>
            <p><strong>Director:</strong> <%= details.Director %></p>
            <p><strong>Actors:</strong> <%= details.Actors %></p>
            <img src="<%= details.Poster %>" alt="<%= movie.title %> Poster">
        </div>
        <div class="poster-bg">
            <img src="<%= details.Poster %>" alt="<%= movie.title %> Poster">
        </div>
    <% } else { %>
        <p>No additional details available.</p>
    <% } %>

    <div class="edit-delete-wrapper">
      <a href="/movie/<%= movie.id %>/edit">Edit</a>
      <form action="/movie/<%= movie.id %>/delete" method="POST" style="display:inline;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this movie?')">Delete</button>
      </form>
    </div>

    <div class="go-back">
      <a href="/">Back to Movies</a>
    </div> 
</body>
</html>
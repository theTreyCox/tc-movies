<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %></title>
    <!-- Bootstrap CSS -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom Styles -->
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header class="container my-4">
        <div class="d-flex justify-content-between align-items-center">
            <a href="/" class="btn btn-secondary">Back to Movies</a>
            <div>
                <a href="/movie/<%= movie.id %>/edit" class="btn btn-warning me-2">Edit</a>
                <form action="/movie/<%= movie.id %>/delete" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this movie?')">Delete</button>
                </form>
            </div>
        </div>
    </header>
    <main class="container">
        <!-- Local Item Details -->
        <div class="row mb-4">
            <div class="col-md-8">
                <h1><%= movie.title %> (<%= movie.release_year %>)</h1>
                <hr>
                <h2>Details for Collection Item</h2>
                <ul class="list-unstyled">
                    <li><strong>Format:</strong> <%= movie.format %></li>
                    <li><strong>Label:</strong> <%= movie.label %></li>
                    <li><strong>Packaging:</strong> <%= movie.packaging %></li>
                    <li><strong>Slipcase:</strong> <%= movie.slipcover ? 'Yes' : 'No' %></li>
                </ul>
            </div>
            <div class="col-md-4 text-center">
                <img src="<%= details.Poster %>" alt="<%= movie.title %> Poster" class="img-fluid rounded">
            </div>
        </div>

        <% if (details) { %>
            <!-- OMDb Details -->
            <hr>
            <div>
                <h2>Details from OMDb</h2>
                <ul class="list-unstyled">
                    <li><strong>Plot:</strong> <%= details.Plot %></li>
                    <li><strong>Genre:</strong> <%= details.Genre %></li>
                    <li><strong>Director:</strong> <%= details.Director %></li>
                    <li><strong>Actors:</strong> <%= details.Actors %></li>
                </ul>
            </div>
            <div class="bg-image-wrapper text-center mt-4 position-absolute">
                <img class="bg-image" src="<%= details.Poster %>" alt="<%= movie.title %> Poster" class="img-fluid rounded">
            </div>
        <% } else { %>
            <p class="text-muted">No additional details available.</p>
        <% } %>
    </main>
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>
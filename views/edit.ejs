<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Edit Movie</title>
        <!-- Bootstrap CSS -->
        <link href="/css/bootstrap.min.css" rel="stylesheet">
        <!-- Custom Styles -->
        <link rel="stylesheet" href="/css/styles.css">
    </head>
<body>
    <main class="container mt-4">
        <h1 class="mb-4">Edit Movie</h1>
        <form action="/movie/<%= movie.id %>/edit" method="POST" class="card p-4">
            <div class="mb-3">
                <label for="title" class="form-label">Title</label>
                <input type="text" name="title" id="title" value="<%= movie.title %>" class="form-control" placeholder="Title" required>
            </div>
            <div class="mb-3">
                <label for="format" class="form-label">Primary Format</label>
                <select name="format" id="format" class="form-select">
                    <option value="4k" <%= movie.format === '4k' ? 'selected' : '' %>>4K</option>
                    <option value="Blu-ray" <%= movie.format === 'Blu-ray' ? 'selected' : '' %>>Blu-ray</option>
                    <option value="DVD" <%= movie.format === 'DVD' ? 'selected' : '' %>>DVD</option>
                    <option value="VHS" <%= movie.format === 'VHS' ? 'selected' : '' %>>VHS</option>
                    <option value="LaserDisc" <%= movie.format === 'LaserDisc' ? 'selected' : '' %>>LaserDisc</option>
                    <option value="Other" <%= movie.format === 'Other' ? 'selected' : '' %>>Other</option>
                </select>
            </div>
            <div class="mb-3">
                <label for="release_year" class="form-label">Release Year</label>
                <input type="number" name="release_year" id="release_year" value="<%= movie.release_year %>" class="form-control" placeholder="Release Year" required>
            </div>
            <div class="mb-3">
                <label for="label" class="form-label">Label</label>
                <input type="text" name="label" id="label" value="<%= movie.label %>" class="form-control" placeholder="Label">
            </div>
            <div class="mb-3">
                <label for="imdb_number" class="form-label">IMDb ID</label>
                <input type="text" name="imdb_number" id="imdb_number" value="<%= movie.imdb_number %>" class="form-control" placeholder="IMDb ID (e.g., tt1234567)" required>
            </div>
            <div class="mb-3">
                <label for="packaging" class="form-label">Packaging</label>
                <select name="packaging" id="packaging" class="form-select">
                    <option value="Standard" <%= movie.packaging === 'Standard' ? 'selected' : '' %>>Standard</option>
                    <option value="SteelBook" <%= movie.packaging === 'Steelbook' ? 'selected' : '' %>>SteelBook</option>
                    <option value="Hard Box" <%= movie.packaging === 'Hard Box' ? 'selected' : '' %>>Hard Box</option>
                    <option value="Other" <%= movie.packaging === 'Other' ? 'selected' : '' %>>Other</option>
                </select>
            </div>
            <div class="form-check mb-3">
                <input type="checkbox" name="slipcover" id="slipcover" class="form-check-input" <%= movie.slipcover ? 'checked' : '' %>>
                <label for="slipcover" class="form-check-label">Includes Slipcover</label>
            </div>
            <div class="d-flex justify-content-between">
                <button type="submit" class="btn btn-success">Save Changes</button>
                <a href="/" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </main>
    <!-- Bootstrap JavaScript -->
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>